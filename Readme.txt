Run data migration to build db
Update-database

Why chose any order
This wasn't specified. The output is the same is the same for different sequences containing the same numbers but in different orders. If the output is the same which is the main 

Why saved verticaly
Numeric data saved in string form is hard to query and manipulate. Alway try and store data in it's purest form as a rule




